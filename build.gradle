buildscript {
    repositories {
        google()  // Menambahkan Google Maven repository
        mavenCentral()  // Menambahkan Maven Central repository (jika diperlukan)
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.4.1'  // Mengatur versi Gradle
        classpath 'com.google.gms:google-services:4.3.15'// Menambahkan dependensi Google Services
    }
}

// Top-level build file where you can add configuration options common to all sub-projects/modules.

// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    id 'com.android.application' version '7.4.0' apply false
    id 'com.android.library' version '7.4.0' apply false
}